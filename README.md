#Ansible-automatic-updates

Ansible Role for automatic updates

This installs and configures unattended-upgrades on Ubuntu servers. It is set up to only allow security updates and does not allow restarts.
